{
  "name": "aether-player",
  "version": "1.0.0",
  "description": "Minimalist audio player with embedded tag metadata & Apple Music UI style",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . AetherPlayer --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "music-metadata": "^7.14.0",
    "sql.js": "^1.10.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-packager": "^17.1.2"
  }
}